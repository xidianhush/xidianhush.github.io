# 4 点多

### 指令

- 指令是计算机执行的最小单位。
- 指令由**操作码**和**操作数**两部分组成：
  - ==**操作码**==：要执行的操作
  -==**操作数**==：用于操作的数据/位置
- 指令被编码为一个$\text{bit}$序列。（就像数据一样）
  - 指令的长度通常是固定的，如16位或32位。
  - 控制单元解释指令：
    生成控制信号序列以执行操作。
  - 操作要么完全执行，要么根本不执行。
- 计算机的指令及其格式被称为
  ==指令集结构== (Instruction Set Architecture，ISA)